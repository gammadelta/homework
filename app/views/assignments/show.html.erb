
		Posted by <%= @user.userName %> <br>
	<%= @assignment.points%>	<%=@assignment.description %> <% if current_user.id == @user.id %>  
	<%= link_to "Settings", edit_assignment_path(@assignment) %><%end %>

