<header class="navbar navbar-inverse">
  <div class="container">
    <%= link_to image_tag("logo.jpg"), '#', id: "logo" %>
    <nav>
      <ul class="nav navbar-nav pull-right">
        <li><%= link_to "Home",   root_path %></li>
        <li><%= link_to "About Us",  '#' %></li>
         <% if current_user %>
         <% m =  Message.where(recever: current_user.id, opened: false).length %>
          <li><%= link_to profile_path do %>Profile <span class="badge" ><%= m %></span> <%end%></li>
          <li> <%= link_to "Sign out", signout_path%></li>
      <% else %>
       <li> <%= link_to "Sign in/Sign up", "/auth/google_oauth2/"%></li>
      <% end %>
      </ul>
    </nav>
  </div>
</header>